public class Zoo {
    /**
     * compile using {@code javac} and run it with {@code Zoo} argument
     * and observe what happen ? (also explain what does it print)*/
    public static void main(String[] args) {
        System.out.println(args[0]);
        System.out.println(args[1]);
    }
}
